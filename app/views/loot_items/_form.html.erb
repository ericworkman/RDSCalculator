<%= form_with(model: [@loot_table, loot_item], local: true) do |form| %>
  <% if loot_item.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(loot_item.errors.count, "error") %> prohibited this loot_item from being saved:</h2>

      <ul>
        <% loot_item.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :probability, style: "display: block" %>
    <%= form.number_field :probability %>
  </div>
  
  <div>
    <%= form.submit %>
  </div>
<% end %>
